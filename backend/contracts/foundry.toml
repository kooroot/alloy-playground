# Minimal Foundry config for a single-contract prototype.
# Run `forge build` inside this directory.
[profile.default]
src = "src"
out = "out"
libs = []
solc_version = "0.8.24"
optimizer = true
optimizer_runs = 200
